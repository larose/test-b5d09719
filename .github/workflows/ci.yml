name: CI

on:
  pull_request:
    branches: [ master ]
  push:
    branches:
      - commit-injection

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@845ca4f

    - name: Check
      run: python3 ci_check.py
